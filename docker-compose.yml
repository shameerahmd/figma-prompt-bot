version: '3.8'

services:
  figma-prompt-optimizer:
    build: .
    container_name: figma-prompt-optimizer
    ports:
      - "8080:8080"
    environment:
      - NVIDIA_API_KEY=${NVIDIA_API_KEY:-}
      - PORT=8080
      - HOST=0.0.0.0
    restart: unless-stopped
